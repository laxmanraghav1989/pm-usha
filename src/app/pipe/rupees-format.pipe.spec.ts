import { RupeesFormatPipe } from './rupees-format.pipe';

describe('RupeesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RupeesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
